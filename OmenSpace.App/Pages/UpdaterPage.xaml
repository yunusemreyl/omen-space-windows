<Page
    x:Class="OmenSpace_App.Pages.UpdaterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24,20,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Updater" FontSize="28" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
            <TextBlock Text="Check for updates to OmenSpace Windows and its background services." FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Spacing="24">
                
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel Spacing="24">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Image Source="ms-appx:///Assets/StoreLogo.png" Width="64" Height="64" Margin="0,0,20,0"/>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="OmenSpace for Windows" FontSize="18" FontWeight="Bold"/>
                                <TextBlock Text="Version 1.0.0 (Release Candidate)" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock x:Name="StatusText" Text="Up to date." Foreground="{ThemeResource SystemFillColorSuccessBrush}" Margin="0,8,0,0"/>
                            </StackPanel>
                            
                            <Button x:Name="BtnCheckUpdates" Grid.Column="2" Content="Check for Updates" VerticalAlignment="Center" Style="{StaticResource AccentButtonStyle}" Click="BtnCheckUpdates_Click"/>
                        </Grid>
                        
                        <ProgressBar x:Name="UpdateProgress" IsIndeterminate="True" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>
                
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                    <StackPanel Spacing="16">
                        <TextBlock Text="Release Notes" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="- Initial Release Candidate for Windows 11&#x0a;- Replaced OmenCore with OmenSpace logic&#x0a;- Implemented Fan Control &amp; Undervolting&#x0a;- Native WinUI 3 Fluent Design with Mica Backdrop" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
